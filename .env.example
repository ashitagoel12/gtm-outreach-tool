# Required: Anthropic API key for Claude API calls
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# HubSpot integration uses a Personal Access Token entered directly in the app UI.
# No HUBSPOT_API_KEY environment variable is needed — the token is user-provided
# at runtime and never stored server-side.
